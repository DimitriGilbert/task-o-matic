#!/usr/bin/env node

/**
 * task-o-matic CLI Binary Entry Point
 *
 * This is the executable entry point for the task-o-matic CLI.
 * It imports and runs the CLI logic from the main index file.
 */

import { runCLI } from '../index';

// Run the CLI
runCLI();
